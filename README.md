# jupyter_setup
setup script for Jupyter notebooks: creates a uv-managed Python venv, installs requirements and registers an IPython kernel

# Usage:
- put required libaries in `requirements.txt`
- set python version in second line in `setup.sh`
